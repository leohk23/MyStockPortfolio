{
  "name": "mystockportfolio",
  "version": "1.0.0",
  "description": "Static stock portfolio dashboard. Prices refreshed hourly by GitHub Actions.",
  "scripts": {
    "extract": "node extract-portfolio.js",
    "publish": "node publish.js",
    "fetch": "node fetch-prices.js",
    "backfill": "node backfill-earnings.js",
    "test": "node fetch-prices.js --selftest && node portfolio.js --selftest && node extract-portfolio.js --selftest && node backfill-earnings.js --selftest",
    "start": "npx --yes serve .",
    "promote": "node -e \"require('fs').copyFileSync('preview/index.html','index.html')\" && echo \"promoted preview -> live\"",
    "preview": "npx --yes serve .",
    "interim": "node check-interim.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/leohk23/MyStockPortfolio.git"
  },
  "license": "ISC",
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
